The following is an explanation of "Active Directory" implemented as part of the project - "Show me the Data Structures"

O(n)| O(n), where n is the depth of the deepest group to which a user belongs

